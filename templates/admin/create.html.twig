{% extends 'base.html.twig' %}

{% block title %}Amin Ajout{% endblock %}

{% block body %}
    <div class="container">
        {{ form_start(form) }}
            {{ form_widget(form) }}

            <button class="btn btn-primary" formnovalidate>
                Ajouter le produit
            </button>
        {{ form_end(form) }}
    </div>
{% endblock %}