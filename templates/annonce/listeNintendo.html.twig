{% extends 'base.html.twig' %}

{% block title %}Super Nintendo{% endblock %}

{% block body %}

<div class="container mt-3">
    {# <div class="row">
        <form class="form-inline">
            <label class="my-1 mr-2" for="inlineFormCustomSelectPref">Trier par Genre</label>
            <select class="custom-select my-1 mr-sm-2" id="inlineFormCustomSelectPref">
                <option selected>Choose...</option>
                {% for cat in allCat %}
                <option value={{ cat.id }}>{{ cat.nom }}</option>
                {% endfor %}
            </select>

            <button type="submit" class="btn btn-primary my-1">Choisir</button>
        </form>
    </div> #}
    <div class="row">
        <div class="col-sm">
            <div class="card">
                <div class="card-body">
                    <div class="row">
                        {% for jeu in allJeux %}
                            <div class="col-lg-3">
                                <div class="card shadow">
                                    <div class="card">
                                        <img class="card-img-top" src="{{ asset( 'images/' ~ jeu.photo  ) }}" alt="Card image cap">
                                        <div class="card-body">
                                            <h5 class="card-title">
                                                <a href="{{ path('jeu_show', {id: jeu.id}) }}"
                                                    class="btn btn-secondary">{{ jeu.titre }}</a>
                                            </h5>
                                            <p><strong>{{ jeu.prix }} €</strong></p>
                                            <p>Année : {{ jeu.annee }}</p>
                                            <p>Categorie : {{ jeu.categorie.nom }}</p>
                                            <p>Console : {{ jeu.consoleId.nom }}</p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                </div>
        </div>


{% endblock %}
